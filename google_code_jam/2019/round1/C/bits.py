#if len(cand) == 500:
    #return 'IMPOSSIBLE'


    #allgames = [p(cand, r) for r in prs]
    #if all('win' == a for a in allgames) return ''.join(cand)
    #if any('tie' == a for a in allgames): return rec(cand, nextmoves, )
    #if any('loose' == a for a in allgames) return ''



    #if 'IMPOSSIBLE' == rec(cand + 'R', prs)
    #rec(cand + 'S', prs)
    #rec(cand + 'P', prs)


#def f(prs):
    #ret = []
    #for i, move in enumerate(zip(*[itertools.cycle(p) for p in prs])):
        #if all(move[0] == m for m in move):
            #return
        #else:
            #return 'IMPOSSIBLE'
        #if i

    #return 'IMPOSSIBLE'

